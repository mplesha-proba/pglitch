on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '*/15 * * * *'

jobs:
  ping_glitch:
    name: Ping glitch
    runs-on: ubuntu-latest
    run: |
      echo 'hi'
 
